        {step === 'dashboard' && customerData && (
          <DashboardView 
            customerData={customerData} 
            onLogout={handleLogout} 
            menuData={menuData}
          />
        )}